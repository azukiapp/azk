#!/usr/bin/env elixir
# vim: ft=elixir

# Azk project paths
azk_path = Path.expand(Path.join([__DIR__, ".."]))

# Start mix
Mix.start

# Load azk/mix.exs and set depspath
Code.load_file Path.join([azk_path, "mix.exs"])
Mix.loadpaths

# Start azk cli and run
Azk.Cli.start(azk_path)
Azk.Cli.run
