#!/bin/bash

__FILE__="${0}"
export AZK_PATH=`cd \`dirname $(readlink ${__FILE__} || echo ${__FILE__} )\`/..; pwd`
export AZK_PATH
export PATH=$AZK_PATH/bin:$PATH

if [ "$1" == "lua" ]; then
  shift
  luajit ${@}
else
  luajit $AZK_PATH/bin/azk.lua ${@}
fi
