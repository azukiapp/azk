var expect = require('chai').expect;
var helper = require('../spec_helper.js');

var cli  = require('../../lib/cli.js')
var fake = helper.prompt_helper

describe("Azk cli init command", function() {
  it("should exist", function(done) {
    //var msg = null;
    //fake.stdout.once('data', function(_msg) {
      //msg = _msg;
    //});

    //fake.stdin.writeNextTick('box\n')
    //cli([null, null, "init"]);
    done();
  });
});
