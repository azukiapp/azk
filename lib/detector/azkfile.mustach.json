{
  // Application id
  "id": "{{&app_id}}",

  "box": "{{&box}}",

  "envs": {
    "dev": {
      // "log_dir": "./log",
      "host": "{{&host}}",
      "alias": [
      ],
      "env": {
      {{#envs}}
        "{{key}}": "{{value}}"
      {{/envs}}
      }
    }
  },

  "build": [
    "# Extra install steps"
  ],

  "services": {
    // Services box depedencies
    // "postgresql": { "box": "azukiapp/postgresql" },
    // "redis": { "box": "azukiapp/redis#0.0.1" },

    "web": {
      "command": "{{&cmd}}"
    }
  }
}
